# State is already separated by environment - different buckets
#  - separate further by using different files for each Snowflake account AND data product

# locals{
#     bucket = ""
#     key = "${local.env}-${local.repo}-${local.account}"
# }
